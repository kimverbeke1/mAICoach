# PadelAnalysis/scraper/requirements-scraper.txt
#
# Extra dependency, enkel nodig voor de GitHub Actions scrape/refresh-
# workflows. Bewust NIET toegevoegd aan het hoofd-requirements.txt van de
# repo, want dat bestand wordt ook gebruikt door de Streamlit Cloud-deploy,
# waar Playwright/browser-binaries niet beschikbaar zijn en dus ook niet
# horen te worden geïnstalleerd.
#
# PADEL_ANALYSIS_PREBUILT_DOCKER_IMAGE_2026-09-19 (op verzoek van Kim: "docker
# image"): playwright staat hier voortaan EXACT gepind (==, niet meer >=) op
# dezelfde versie als de FROM-tag in PadelAnalysis/scraper/Dockerfile
# (mcr.microsoft.com/playwright/python:v1.62.0-noble). Die basis-image bevat
# AL de Chromium-browserbinary die precies bij playwright==1.62.0 hoort - een
# lossere >=-constraint zou pip toelaten om een NIEUWERE playwright-package
# te installeren dan de browserbinary die al in het image zit, wat een
# "Executable doesn't exist"-fout geeft bij het draaien van de scraper.
# Bij een toekomstige upgrade: verhoog deze versie EN de FROM-tag in
# Dockerfile ALTIJD SAMEN, nooit apart.
playwright==1.62.0
