# PadelAnalysis/scraper/requirements-scraper.txt
#
# Extra dependency, enkel nodig voor de GitHub Actions scrape/refresh-
# workflows. Bewust NIET toegevoegd aan het hoofd-requirements.txt van de
# repo, want dat bestand wordt ook gebruikt door de Streamlit Cloud-deploy,
# waar Playwright/browser-binaries niet beschikbaar zijn en dus ook niet
# horen te worden geïnstalleerd.
#
# PADEL_ANALYSIS_SLIM_IMAGE_2026-09-21 (op verzoek van Kim, over trage
# "Initialize containers"-stappen bij de GitHub Actions-workflows): sinds het
# slanke, zelfgebouwde Dockerfile (python:3.12-slim + zelf Chromium
# installeren, GEEN vooraf-gebouwd multi-browser Playwright-basisimage meer)
# is deze versie NIET langer gekoppeld aan een specifieke FROM-tag in
# Dockerfile - `playwright install --with-deps chromium` gebeurt daar in
# DEZELFDE RUN-laag, ONMIDDELLIJK na deze pip-install, dus de geïnstalleerde
# Chromium-browserbinary komt altijd automatisch overeen met de hier
# gepinde playwright-versie. Een upgrade van deze versie vereist dus geen
# aparte aanpassing elders meer (enkel een nieuwe image-build, die
# automatisch getriggerd wordt door een wijziging aan dit bestand - zie
# .github/workflows/build-scraper-image.yml).
playwright==1.62.0
